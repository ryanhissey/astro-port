---
interface Props {
	size?: 'sm' | 'md';
}

const { size = 'md' } = Astro.props;
---

<span class:list={[
	'rounded-full bg-brand-600/60 ring-1 ring-brand-500/30 font-medium text-brand-100',
	size === 'sm' ? 'px-2.5 py-0.5 text-xs' : 'px-3 py-1 text-sm',
]}>
	<slot />
</span>
